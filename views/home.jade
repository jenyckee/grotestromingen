extends layout

block content
  include ./navbar.jade
  .container
    .row
      .col-md-12
        #banner.page-header
          h1 Examenvragen Grote Stromingen
    .row
      table.table.table-striped.table-hover
        thead
          tr
            td 
        tbody
          each lesson in lessons
            tr
              td #{lesson.id} 
   script.
      var data = !{JSON.stringify(lessons)}